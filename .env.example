# One connection string — from Neon, Vercel Postgres, Supabase, etc.
# Use the "pooled" or "serverless" URL when the host offers it (better on Vercel).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
